
Surface an inline Connect button so the user can authorize an OAuth integration.

## Parameters

<ParamField type="string">
  The integration to connect, e.g. 'notion', 'gmail', 'google\_calendar', 'github', 'x', 'mcp:linear'. Must match one of the supported slugs (registered Pipedream app, 'x', 'github', or 'mcp:\<slug>').
</ParamField>
