
Learn about Zo's tools

Zo has access to a wide range of tools to operate your computer, use your integrations, and navigate the internet.

You can type @ in the chat to mention a specific tool. You can also simply refer to these tools loosely by name, e.g. “use your image search tool”. Mentioning specific tools can make your prompts more precise and reliable.

## <Icon icon="folder-tree" /> Files

* [`Create file`](/tools/create-or-rewrite-file) – Create a new file or rewrite an existing one with the provided content.
* [`Edit file`](/tools/edit-file) – Edit a text file using a sequence of precise edit operations.
* [`Edit file fast`](/tools/edit-file-llm) – Edit a text file using a fast apply LLM.
* [`Read file`](/tools/read-file) – Read a file from the computer.
* [`List files`](/tools/list-files) – Directory listing tool that shows files and subdirectories in a tree structure, helping you explore and understand the project organization.
* [`Search files`](/tools/grep-search) – Search files by content or filename using ripgrep.
* [`Transcribe audio`](/tools/transcribe-audio) – Transcribe an audio file and save the transcript next to the original file.
* [`Transcribe video`](/tools/transcribe-video) – Transcribe audio from a video file and save the transcript next to the original file.
* [`Generate image`](/tools/generate-image) – Generate an image following the provided prompt using an AI image generation model.
* [`Edit image`](/tools/edit-image) – Remix an existing image (or images) using an AI image model.
* [`Generate video from image`](/tools/generate-video) – Generate a short, 5 second video from an input image using an AI video generation model.
* [`Generate diagram`](/tools/generate-d2-diagram) – Generate a block diagram, using D2.

## <Icon icon="globe" /> Web

* [`Read webpage`](/tools/read-webpage) – Read the content of a webpage.
* [`Open webpage`](/tools/open-webpage) – Open a webpage in Zo's browser.
* [`View webpage`](/tools/view-webpage) – View the current page in the browser and get its content plus a screenshot.
* [`Use webpage`](/tools/use-webpage) – Interact with the current page using an AI agent.
* [`Save webpage`](/tools/save-webpage) – Save webpage content to the user's Articles directory.
* [`Search the web`](/tools/web-search) – Search the web using a search engine.
* [`Research the web`](/tools/web-research) – Perform an in-depth web search using a more powerful search engine.
* [`Find similar links`](/tools/find-similar-links) – Find web pages similar to a given URL.
* [`Search images`](/tools/image-search) – Search for images on the web using a search engine.
* [`Search X`](/tools/x-search) – Search X (Twitter) for posts and users matching a query.
* [`Search Google Maps`](/tools/maps-search) – Search Google Maps for locations.

## <Icon icon="computer" /> Computer

* [`Run command`](/tools/bash) – Run a single shell command on the computer.
* [`Run parallel commands`](/tools/run-parallel-cmds) – Run multiple shell commands in parallel on the computer.
* [`Run commands sequentially`](/tools/run-sequential-cmds) – Run multiple shell commands sequentially on the computer.
* [`Create site`](/tools/create-website) – Create a site (also known as an app, or a website).
* [`Publish site`](/tools/publish-site) – Publish or republish a Zo Site so it gets a dedicated URL and runs a production build.
* [`Unpublish site`](/tools/unpublish-site) – Unpublish a Zo Site, removing its dedicated URL.
* [`Create service`](/tools/register-user-service) – Register a managed User Service on your Zo server.
* [`List services`](/tools/list-user-services) – List managed User Services running on your Zo server.
* [`Diagnose service`](/tools/service-doctor) – Diagnose issues and get details about the status of a user\_service.
* [`Update service`](/tools/update-user-service) – Update a managed user service on your Zo server. Only specify the fields you want to change; omitted fields keep their existing values. Always restarts the service process.
* [`Delete service`](/tools/delete-user-service) – Delete a managed user service on your Zo server.
* [`Proxy local service`](/tools/proxy-local-service) – Proxy a local service to a public endpoint using a TCP tunnel, similar to ngrok.
* [`Use device`](/tools/use-device) – Run a command on the user's desktop device via the Zo desktop app.

## <Icon icon="plug-zap" /> Integrations

* [`Send Discord message`](/tools/send-discord-message) – Send a message to the user via Discord.
* [`Send Slack message`](/tools/send-slack-message) – Send a message to the user via Slack.
* [`Get Slack info`](/tools/get-slack-info) – Look up Slack workspaces, channels, and members.
* [`Get Slack history`](/tools/get-slack-history) – Get message history from a Slack channel or thread.
* [`Get Discord history`](/tools/get-discord-history) – Get message history from a Discord channel, thread, or DM.
* [`Get Discord info`](/tools/get-discord-info) – Look up Discord servers, channels, threads, or members.
* [`Send email`](/tools/send-email-to-user) – Send an email to the user's email address.
* [`Send SMS`](/tools/send-sms-to-user) – Send an SMS text message to the USER or a contact.
* [`Send Telegram message`](/tools/send-telegram-message) – Send a message to the user via Telegram.
* [`Connect Telegram`](/tools/connect-telegram) – Generate a pairing code to connect the user's Telegram account to Zo.
* [`List app tools`](/tools/list-app-tools) – List all available tools and configured props for an app connected by the user.
* [`Connect integration`](/tools/connect-integration) – Surface an inline Connect button so the user can authorize an OAuth integration.
* [`Search app catalog`](/tools/search-app-catalog) – Search Pipedream's full catalog of 1000+ app integrations by keyword.
* [`Use integration`](/tools/use-integration) – Run an action on a connected long-tail Pipedream catalog app.
* [`Use Gmail`](/tools/use-app-gmail) – Work with the user's connected Gmail account.
* [`Use Google Calendar`](/tools/use-app-google-calendar) – Work with the user's connected Google Calendar account.
* [`Use Google Drive`](/tools/use-app-google-drive) – Work with the user's connected Google Drive account (includes Google Docs, Sheets, Slides, and Forms).
* [`Use Google Sheets`](/tools/use-app-google-sheets) – Work with the user's connected Google Sheets spreadsheets.
* [`Use Google Tasks`](/tools/use-app-google-tasks) – Work with the user's connected Google Tasks account.
* [`Use OneDrive`](/tools/use-app-microsoft-onedrive) – Work with the user's connected Microsoft OneDrive account via Pipedream.
* [`Use Outlook`](/tools/use-app-microsoft-outlook) – Work with the user's connected Microsoft Outlook account.
* [`Use Outlook Calendar`](/tools/use-app-microsoft-outlook-calendar) – Work with the user's connected Microsoft Outlook Calendar account.
* [`Use Notion`](/tools/use-app-notion) – Work with the user's connected Notion account.
* [`Use Linear`](/tools/use-app-linear) – Work with the user's connected Linear workspace.
* [`Use Airtable`](/tools/use-app-airtable) – Work with the user's connected Airtable bases.
* [`Use Dropbox`](/tools/use-app-dropbox) – Work with the user's connected Dropbox account via Pipedream.
* [`Use Spotify`](/tools/use-app-spotify) – Use a tool to work with the user's connected Spotify account.
* [`Use X`](/tools/use-app-x) – Work with the user's connected X (Twitter) account.

## <Icon icon="credit-card" /> Commerce

* [`Create product`](/tools/create-stripe-product) – Create a Stripe product on the user's connected account.
* [`Create price`](/tools/create-stripe-price) – Create a new price for an existing Stripe product.
* [`Create payment link`](/tools/create-stripe-payment-link) – Create a payment link for an existing Stripe price.
* [`List payment links`](/tools/list-stripe-payment-links) – List all payment links for the user.
* [`Update payment link`](/tools/update-stripe-payment-link) – Update an existing payment link (activate/deactivate).
* [`Update product`](/tools/update-stripe-product) – Update an existing Stripe product's name or description.
* [`List orders`](/tools/list-stripe-orders) – List all orders (checkout sessions) for the user.
* [`Update orders`](/tools/update-stripe-orders) – Update the fulfillment status of one or more orders.

## <Icon icon="clock" /> Automations

* [`Create automation`](/tools/create-automation) – Create an automation to run an AI task on a schedule.
* [`List automations`](/tools/list-automations) – List all registered automations.
* [`Get automation`](/tools/get-automation) – Get full details of a single automation, including the complete instruction text.
* [`Edit automation`](/tools/edit-automation) – Edit an existing automation's title, instruction, schedule, preferred communication channel, or model.
* [`Delete automation`](/tools/delete-automation) – Delete an existing automation.

## <Icon icon="user" /> Personalization

* [`Create persona`](/tools/create-persona) – Create a new persona for Zo.
* [`List personas`](/tools/list-personas) – List all personas for the user.
* [`Edit persona`](/tools/edit-persona) – Edit fields of an existing persona. Heavily biased toward additive, surgical changes — there is no full-replacement option.
* [`Delete persona`](/tools/delete-persona) – Delete a persona by ID for the current user.
* [`Set active persona`](/tools/set-active-persona) – Set the active persona for the current channel (chat, SMS, email, etc.).
* [`List available scopes`](/tools/list-available-scopes) – List all available tool permission scopes, presets, and requirement rules.
* [`Set persona scopes`](/tools/set-persona-scopes) – Set which tools a persona is allowed to use.
* [`Create rule`](/tools/create-rule) – Create a new user rule for Zo.
* [`List rules`](/tools/list-rules) – List all rules for the user.
* [`Edit rule`](/tools/edit-rule) – Edit an existing rule.
* [`Delete rule`](/tools/delete-rule) – Delete a rule by ID.
* [`Update user settings`](/tools/update-user-settings) – Update a user setting (name or bio) in Zo.

## <Icon icon="orbit" /> Zo Space

* [`Read from space`](/tools/get-space-route) – Get a space route by path, including its source code.
* [`Write space route`](/tools/write-space-route) – Create a new route or fully rewrite an existing one in the user's zo.space site.
* [`Edit space route`](/tools/edit-space-route) – Edit an existing route's code by supplying only the changed sections.
* [`Delete from space`](/tools/delete-space-route) – Delete a route from the user's zo.space site.
* [`View space`](/tools/list-space-routes) – List all routes in the user's zo.space site.
* [`Undo space change`](/tools/undo-space-route) – Undo the last change to a space route, restoring the previous version.
* [`Redo space change`](/tools/redo-space-route) – Redo a previously undone space route change, restoring the next version.
* [`View route history`](/tools/get-space-route-history) – View the full version history of a space route.
* [`Upload asset to space`](/tools/update-space-asset) – Copy a file from the workspace to the user's zo.space assets.
* [`Remove asset from space`](/tools/delete-space-asset) – Delete a static asset from the user's zo.space site.
* [`List space assets`](/tools/list-space-assets) – List all uploaded assets in the user's zo.space site.
* [`Check space`](/tools/get-space-errors) – Get recent errors from the user's zo.space routes.
* [`Restart space`](/tools/restart-space-server) – Clean reinstall dependencies, rebuild, and restart the zo.space server.
* [`Get space settings`](/tools/get-space-settings) – Get space settings for the user's zo.space site.
* [`Update space settings`](/tools/update-space-settings) – Update space settings — space-globally or for a single page route.
